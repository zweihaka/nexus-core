# nexus-core
Infrastructure lab focused on DevOps technology stack
There's a reverse-proxy nginx webserver that you need to install on your machine and set like a daemon. The config of that reverse-proxy lies in "default" file, that you need to move/copy to /etc/nginx/sites-available/default

